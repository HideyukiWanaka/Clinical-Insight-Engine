# 1. Python
python3 --version          # 3.11以上

# 2. R
Rscript --version          # 4.3以上

# 3. Rパッケージ（Rコンソールで実行）
# Rscript -e "library(tidyverse)"  などで確認

# 4. APIキー登録
python3 -c "import keyring; print(keyring.get_password(
    'CIE_Platform_Anthropic_OAuth_Key', 'api_key'))"
# 登録済みならキーが表示される

# 5. Pythonパッケージ
pip install pydantic sqlalchemy aiosqlite httpx keyring pyyaml jsonschema streamlit pytest

# 6.（オプション）Docker
docker info

# 7.（オプション）spaCy
python3 -c "import spacy; spacy.load('ja_core_news_sm')"
